apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  selector:
    matchLabels:
      app: guestbook
      tier: frontend
  replicas: 10
  template:
    metadata:
      labels:
        app: guestbook
        tier: frontend
    spec:
      containers:
      - name: php-redis
        image: php-redis 
        #gcr.io/google-samples/gb-frontend:v4
        resources:
          requests:
            cpu: 105m
            memory: 100Mi
        ports:
        - containerPort: 80
